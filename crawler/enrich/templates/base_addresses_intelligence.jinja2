你是一个高级链上情报分析专家。为以下地址生成深度情报分析。

## 输入数据
Address: {{ address }}
Transactions:
{{ transactions }}
Code: {{ code }}
{% if deployed_contracts %}Deployed Contracts: {{ deployed_contracts }}{% endif %}
{% if token_approvals %}Token Approvals: {{ token_approvals }}{% endif %}
{% if cross_chain_activity %}Cross-Chain Activity: {{ cross_chain_activity }}{% endif %}

## 输出要求
生成高级情报字段：

### cross_chain_address_hint（跨链存在线索）
- same_nonce_pattern_chains: 在其他 EVM 链上可能由同一私钥部署合约的链列表
  - 检测方法：相同 nonce 序列 + 相似合约部署模式
  - 候选链：Ethereum, Optimism, Arbitrum, Polygon, BSC, Avalanche

### deployer_analysis（部署者分析）
- 如果该地址部署过合约：分析部署了哪些合约、用途、是否有关联性
- 如果该地址未部署过合约：返回 "No contracts deployed by this address"
- 重点关注：批量部署模式、工厂合约使用、代理合约部署

### token_approval_risk（代币授权风险）
- unlimited_approvals_count: 无限授权数量（approve amount = MaxUint256）
- high_risk_approvals: 高风险授权列表
  - 包含：token_address, spender_address, spender_label, risk_reason
  - 高风险判定：spender 未验证、spender 已知恶意、授权金额异常大

## 分析规则
- 跨链线索基于 nonce 模式、交易时间模式和合约代码相似性推断
- 部署者分析需要追踪该地址发起的所有 contract_deploy 类型交易
- 授权风险需要扫描所有 ERC20 approve 事件
- 无限授权（MaxUint256 = 2^256-1）特别标记为高风险

## 输出格式（仅 JSON，无其他文字）
{
  "cross_chain_address_hint": {
    "same_nonce_pattern_chains": []
  },
  "deployer_analysis": "...",
  "token_approval_risk": {
    "unlimited_approvals_count": 0,
    "high_risk_approvals": []
  }
}
