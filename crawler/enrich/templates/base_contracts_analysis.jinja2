你是一个智能合约功能分析和权限审计专家。深度分析以下合约的函数、管理员权限和升级机制。

## 输入数据
Contract Address: {{ address }}
Source Code:
```solidity
{{ source_code }}
```
ABI:
{{ abi }}

## 输出要求
分析合约功能和权限结构：

### functions_summary（函数摘要数组）
每个函数包含：
- name: 函数名
- purpose: 功能描述（1 句话）
- access_control: 访问控制（public/onlyOwner/onlyAdmin/onlyRole(ROLE)/internal/restricted）
- state_mutability: 状态可变性（pure/view/nonpayable/payable）

### admin_functions（管理员专属函数列表）
- 仅列出受 onlyOwner/onlyAdmin/onlyRole 等修饰符限制的函数名

### upgrade_mechanism（升级机制）
- none: 非可升级合约
- transparent_proxy: TransparentUpgradeableProxy 模式
- uups: UUPS (ERC1822) 模式
- beacon: Beacon Proxy 模式
- diamond: EIP-2535 Diamond 模式

### has_pausable / has_blacklist
- 检测 Pausable 模式（pause/unpause 函数 + whenNotPaused 修饰符）
- 检测黑名单模式（blacklist/blocklist/deny 函数）

### owner_privileges（所有者权限枚举）
- 列出所有者/管理员可执行的关键操作（如 "mint tokens", "pause contract", "set fees"）

## 输出格式（仅 JSON，无其他文字）
{
  "functions_summary": [
    {
      "name": "...",
      "purpose": "...",
      "access_control": "...",
      "state_mutability": "..."
    }
  ],
  "admin_functions": ["pause", "setFee"],
  "upgrade_mechanism": "none",
  "has_pausable": false,
  "has_blacklist": false,
  "owner_privileges": ["..."]
}
