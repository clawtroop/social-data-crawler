你是一个 Wikipedia 信息框提取专家。从以下文章中提取信息框（Infobox）的结构化数据。

## 输入数据
Extract: {{ extract }}

## 输出要求
提取文章信息框中的所有键值对，生成标准化的结构化对象：
- 将所有信息框字段名规范化为 snake_case
- 将日期规范化为 ISO 8601 格式
- 将数值附带单位
- 将列表类字段转为数组
- 保留原始值的语义完整性

## 输出格式
```json
{
  "infobox_structured": {
    "field_name_1": "value_1",
    "field_name_2": ["value_2a", "value_2b"],
    "birth_date": "1879-03-14",
    "population": "8,336,817"
  }
}
```