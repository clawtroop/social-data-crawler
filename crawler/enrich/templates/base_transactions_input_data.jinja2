你是一个区块链交易解码专家。将以下交易的 input data 解码为人类可读的函数调用和参数。

## 输入数据
To Address: {{ to_address }}
Input Data (hex): {{ input }}
{% if abi %}Contract ABI: {{ abi }}{% endif %}

## 输出要求
解码交易 input data，生成：
- function_name: 函数名称（如 "swap"、"transfer"、"approve"）。如果无法解码，返回 "unknown"
- function_signature: 完整函数签名（如 "swap(address,uint256,uint256,address,uint256)"）
- decoded_parameters: 解码后的参数对象，key 为参数名，value 为参数值
- human_readable_action: 用通俗语言描述这笔交易做了什么（1-2句话）

## 解码规则
- 前 4 字节（8 个 hex 字符）为函数选择器
- 常见选择器：0xa9059cbb (transfer)、0x095ea7b3 (approve)、0x38ed1739 (swapExactTokensForTokens)
- 如果 input 为 "0x" 或为空，则为普通 ETH 转账
- 地址参数去掉前导零，显示为标准 0x 格式
- 数值参数根据 token decimals 转换为人类可读数值

## 输出格式（仅 JSON，无其他文字）
{
  "function_name": "...",
  "function_signature": "...",
  "decoded_parameters": {},
  "human_readable_action": "..."
}
