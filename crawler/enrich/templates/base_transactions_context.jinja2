你是一个区块链 MEV 和交易上下文分析专家。检测以下交易的 MEV 活动、合约交互和关联交易。

## 输入数据
Transaction Hash: {{ tx_hash }}
Block Number: {{ block_number }}
From Address: {{ from_address }}
To Address: {{ to_address }}
Input Data (hex): {{ input }}
Value (wei): {{ value }}
Gas Price (wei): {{ gas_price }}
{% if block_transactions %}Block Transactions Context: {{ block_transactions }}{% endif %}

## 输出要求
分析交易上下文，生成：
- is_mev: 是否涉及 MEV（最大可提取价值）
- mev_type: MEV 类型（仅当 is_mev 为 true 时）
- is_contract_interaction: 是否与智能合约交互
- is_internal_tx_parent: 是否会产生内部交易
- related_tx_hashes: 关联交易哈希列表（如三明治攻击的配对交易）

## MEV 类型枚举
frontrun, backrun, sandwich, liquidation, arbitrage

## 检测规则
- **三明治攻击 (sandwich)**: 同一区块中，同一 DEX 池的买-目标-卖模式，攻击者 gas price 异常高
- **套利 (arbitrage)**: 同一区块中跨多个 DEX 的代币兑换形成循环
- **清算 (liquidation)**: 调用借贷协议的 liquidate/liquidatePosition 函数
- **抢跑 (frontrun)**: 异常高 gas price + 在同类交易前执行
- **尾随 (backrun)**: 紧跟在大额交易后的同类操作
- **合约交互**: input 长度 > 2（非 "0x"）则为合约交互
- **内部交易**: 调用合约且该合约可能触发其他合约调用

## 输出格式（仅 JSON，无其他文字）
{
  "is_mev": false,
  "mev_type": null,
  "is_contract_interaction": true,
  "is_internal_tx_parent": false,
  "related_tx_hashes": []
}
