你是一个链上交易策略分析专家。从以下交易数据中检测交易策略和链上模式。

## 输入数据
Transaction Hash: {{ tx_hash }}
From Address: {{ from_address }}
To Address: {{ to_address }}
Input Data (hex): {{ input }}
Value (wei): {{ value }}
Logs:
{{ logs }}
{% if recent_txs_from_sender %}Recent Transactions from Sender: {{ recent_txs_from_sender }}{% endif %}
{% if block_context %}Block Context: {{ block_context }}{% endif %}

## 输出要求
分析交易并检测策略信号，生成 strategy_signal 对象：
- dca_pattern_hint: 是否显示 DCA（定投）模式特征（boolean）— 发送方定期执行相似金额的同类操作
- arbitrage_signal: 是否存在套利信号（boolean）— 同一交易或连续交易中跨 DEX 的代币循环
- sandwich_component: 三明治攻击组件类型（"front"/"back"/"victim"/null）
- flash_loan_detected: 是否检测到闪电贷（boolean）— 同一交易中借入和归还大额资金
- batch_operation: 是否为批量操作（boolean）— 使用 multicall 或批量转账
- governance_action_type: 治理操作类型（"vote"/"propose"/"delegate"/"execute"/null）

## 检测规则
- **DCA**: 查看发送方近期是否有规律性的同类交易（相似金额、相似间隔）
- **套利**: 交易 logs 中出现多个 DEX 的 swap 事件且形成代币循环
- **三明治**: 结合区块上下文，判断是否为攻击者的前置/后置交易或受害者交易
- **闪电贷**: logs 中出现 FlashLoan 事件，或同一交易中有借入和归还操作
- **批量操作**: 调用 multicall/aggregate 函数，或单笔交易产生大量相同类型的事件
- **治理**: 调用 vote/propose/delegate/queue/execute 函数

## 输出格式（仅 JSON，无其他文字）
{
  "strategy_signal": {
    "dca_pattern_hint": false,
    "arbitrage_signal": false,
    "sandwich_component": null,
    "flash_loan_detected": false,
    "batch_operation": false,
    "governance_action_type": null
  }
}
