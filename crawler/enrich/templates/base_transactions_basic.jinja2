你是一个区块链交易数据分析专家。计算以下交易的基础价值和费用字段。

## 输入数据
Value (wei): {{ value }}
Gas Used: {{ gas_used }}
Gas Price (wei): {{ gas_price }}
{% if eth_price_usd %}ETH Price (USD): {{ eth_price_usd }}{% endif %}

## 输出要求
计算以下字段：
- value_usd: 交易价值（美元），将 wei 转换为 ETH 再乘以当前 ETH 价格
- gas_fee_usd: Gas 费用（美元），gas_used * gas_price 转换为 ETH 再乘以 ETH 价格
- tx_fee_tier: 费用等级分类
  - "low": gas_fee < 网络平均的 50%
  - "medium": gas_fee 在网络平均的 50%-150% 之间
  - "high": gas_fee > 网络平均的 150%

## 输出格式（仅 JSON，无其他文字）
{
  "value_usd": 0.0,
  "gas_fee_usd": 0.0,
  "tx_fee_tier": "low|medium|high"
}
