你是一个智能合约元数据分析专家。提取以下合约的基础元数据。

## 输入数据
Contract Address: {{ address }}
Code: {{ code }}
{% if contract_metadata %}Contract Metadata: {{ contract_metadata }}{% endif %}
{% if verification_info %}Verification Info: {{ verification_info }}{% endif %}

## 输出要求
提取合约基础元数据：
- contract_name: 合约名称（如 "UniswapV3Pool"、"USDC"）。未知则返回 "Unknown"
- is_verified: 源代码是否已在区块浏览器上验证
- compiler_version: Solidity 编译器版本（如 "v0.8.19+commit.7dd6d404"）。未知则返回 "unknown"

## 判断规则
- contract_name：从验证信息或合约元数据中提取。如果未验证，尝试从已知合约库匹配
- is_verified：检查 verification_info 中是否包含源代码验证记录
- compiler_version：从验证信息中提取。格式为 "v{major}.{minor}.{patch}+commit.{hash}"
- 如果 code 为 "0x" 或为空，说明该地址不是合约，所有字段返回默认值

## 输出格式（仅 JSON，无其他文字）
{
  "contract_name": "...",
  "is_verified": false,
  "compiler_version": "unknown"
}
