此字段组由 embedding 模型生成，非 LLM 直接输出。

## 说明
arxiv_embeddings 字段组包含以下向量嵌入，由专用的 embedding 模型（如 text-embedding-3-small, BGE, E5 等）生成：

- title_embedding: 标题的向量嵌入（array<number>）
- abstract_embedding: 摘要的向量嵌入（array<number>）
- full_paper_embedding: 全文的向量嵌入（array<number>，可选）

## 输入数据
Title: {{ title }}
Abstract: {{ abstract }}

## 处理流程
1. 将 title 文本送入 embedding 模型，生成 title_embedding
2. 将 abstract 文本送入 embedding 模型，生成 abstract_embedding
3. 如有全文，将完整文本送入 embedding 模型，生成 full_paper_embedding

注意：此模板仅作说明用途，实际嵌入由 embedding pipeline 生成，不经过 LLM 处理。