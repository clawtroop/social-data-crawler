你是一个区块链数据关联分析专家。从以下交易数据中提取可跨数据集关联的标识符。

## 输入数据
To Address: {{ to_address }}
Input Data (hex): {{ input }}
{% if protocol_name %}Protocol Name: {{ protocol_name }}{% endif %}
{% if contract_name %}Contract Name: {{ contract_name }}{% endif %}
{% if tx_type %}Transaction Type: {{ tx_type }}{% endif %}

## 输出要求
提取跨数据集可关联的标识符：
- protocol_website_hint: 协议官方网站 URL（如可推断）
- protocol_github_hint: 协议 GitHub 仓库 URL（如可推断）
- protocol_wikipedia_hint: 协议 Wikipedia 页面标题（如可推断）

## 提取规则
- 根据 to_address 匹配已知协议合约，推断其官网和 GitHub
- 根据 protocol_name 推断相关链接
- 主流协议示例：
  - Uniswap → uniswap.org, github.com/Uniswap
  - Aave → aave.com, github.com/aave
  - Aerodrome → aerodrome.finance, github.com/aerodrome-finance
  - Compound → compound.finance, github.com/compound-finance
- 如无法确定，对应字段返回 null
- 不要猜测不确定的链接，宁可返回 null

## 输出格式（仅 JSON，无其他文字）
{
  "linkable_identifiers": {
    "protocol_website_hint": null,
    "protocol_github_hint": null,
    "protocol_wikipedia_hint": null
  }
}
