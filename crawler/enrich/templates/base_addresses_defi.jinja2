你是一个 DeFi 头寸分析专家。枚举以下地址持有的 DeFi 头寸。

## 输入数据
Address: {{ address }}
DeFi Positions Raw Data:
{{ defi_positions_raw }}

## 输出要求
枚举所有 DeFi 头寸，每个头寸包含：
- protocol: 协议名称（如 "Aerodrome", "Aave", "Compound"）
- pool: 池子名称或标识（如 "ETH/USDC 0.3%"）
- position_type: 头寸类型
- token_pair: 代币对（如 "ETH/USDC"）
- value_usd: 当前价值（美元）
- apy_at_entry: 进入时的年化收益率
- pnl_estimated: 预估盈亏（美元）

## 头寸类型枚举
liquidity_provision, lending_supply, lending_borrow, staking, yield_farming, leverage_position, options, perpetual

## 分析规则
- 从 raw data 中识别不同协议的头寸格式
- LP 头寸需要计算当前价值（包括无常损失）
- 借贷头寸需要区分存款（supply）和借款（borrow）
- 质押头寸包括原始质押金额和累积奖励
- 如果 APY 无法确定，返回 null
- PnL 计算基于进入价值 vs 当前价值
- 按 value_usd 降序排列

## 输出格式（仅 JSON，无其他文字）
{
  "defi_positions": [
    {
      "protocol": "...",
      "pool": "...",
      "position_type": "...",
      "token_pair": "...",
      "value_usd": 0.0,
      "apy_at_entry": null,
      "pnl_estimated": 0.0
    }
  ]
}
